import React, {useEffect, useState} from 'react'
import { Text, ScrollView } from 'react-native'

const OneMedicalCenter = () => {

    useEffect(() => {

    },[]);

  return (
    <ScrollView>
        <Text>One Medical Center</Text>
    </ScrollView>
  )
}

export default OneMedicalCenter